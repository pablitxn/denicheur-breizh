# Server only. Never use VITE_ prefixes for these values.
XAI_API_KEY=
FIRECRAWL_API_KEY=
COLLECTOR_PORT=4315
COLLECTOR_XAI_BUDGET_USD=25
COLLECTOR_FIRECRAWL_BUDGET_CREDITS=5000
# Set only after checking the promotion in each provider's console (ISO 8601).
COLLECTOR_XAI_EXPIRES_AT=
COLLECTOR_FIRECRAWL_EXPIRES_AT=
